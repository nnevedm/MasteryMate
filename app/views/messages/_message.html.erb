<div id="message-<%= message.id %>" class="message-style">
  <small>
    <strong style="margin-right: 8px"><%= message.user.first_name %></strong>
    <i><%= message.created_at.strftime("at %l:%M %p") %></i>
  </small>
  <p class="m-2"><%= message.content %></p>
</div>
