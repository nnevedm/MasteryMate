<div id="message-<%= message.id %>" class="message-style">
  <small>
    <i><strong><%= message.user.first_name %></strong>
    <%= message.created_at.strftime("at %l:%M %p") %></i>
  </small>
  <p class="m-0"><%= message.content %></p>
</div>
